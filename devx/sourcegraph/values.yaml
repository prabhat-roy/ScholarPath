# Sourcegraph (OSS) — universal code search across ScholarPath
image: { repository: sourcegraph/server, tag: 5.5.0 }
sourcegraph:
  externalURL: https://sourcegraph.scholarpath.internal
  repositoriesToAdd: [github.com/prabhat-roy/ScholarPath]
ingress: { enabled: true, hosts: [{ host: sourcegraph.scholarpath.internal }] }
