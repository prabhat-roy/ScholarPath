apiVersion: temporal.io/v1beta1
kind: TemporalNamespace
metadata: { name: scholarpath, namespace: temporal-system }
spec:
  retentionPeriod: 30d
  archival:
    history:    { state: enabled, uri: s3://scholarpath-temporal-archive/history }
    visibility: { state: enabled, uri: s3://scholarpath-temporal-archive/visibility }
