global:
  novuVersion: 1.0.0
api: { ingress: { enabled: true, hosts: [{ host: novu-api.scholarpath.internal }] } }
web: { ingress: { enabled: true, hosts: [{ host: novu.scholarpath.internal }] } }
mongodb: { enabled: true }
redis:   { enabled: true }
