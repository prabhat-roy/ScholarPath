image: { repository: trinodb/trino, tag: 462 }
coordinator: { jvm: { maxHeapSize: 8G } }
worker:      { replicas: 3, jvm: { maxHeapSize: 16G } }
catalogs:
  postgresql: |
    connector.name=postgresql
    connection-url=jdbc:postgresql://postgres-scholarpath/scholarpath
  clickhouse: |
    connector.name=clickhouse
    connection-url=jdbc:clickhouse://clickhouse:8123/scholarpath_analytics
ingress:
  enabled: true
  hosts: [{ host: trino.scholarpath.internal, paths: [{ path: /, pathType: Prefix }] }]
