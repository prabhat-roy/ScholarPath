# Apache Iceberg — open table format for huge analytic datasets
catalog:
  type: rest
  uri: http://polaris.scholarpath.svc:8181
  warehouse: s3://scholarpath-iceberg-warehouse/
  s3: { endpoint: http://minio.scholarpath.svc:9000, access_key: __MINIO_ACCESS__, secret_key: __MINIO_SECRET__ }
