apiVersion: policy.sigstore.dev/v1beta1
kind: ClusterImagePolicy
metadata: { name: scholarpath-cosign }
spec:
  images:
    - glob: "harbor.scholarpath.internal/scholarpath/*"
  authorities:
    - name: keyless-github
      keyless:
        url: https://fulcio.sigstore.dev
        identities:
          - issuer: https://token.actions.githubusercontent.com
            subjectRegExp: 'https://github.com/prabhat-roy/ScholarPath/.*'
