apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: scholarpath-azure
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/prabhat-roy/ScholarPath.git
        revision: HEAD
        directories:
          - path: helm/charts/*
  template:
    metadata:
      name: '{{path.basename}}-azure'
      labels: { cloud: azure, scholarpath.io/managed-by: argocd }
    spec:
      project: scholarpath
      source:
        repoURL: https://github.com/prabhat-roy/ScholarPath.git
        targetRevision: HEAD
        path: '{{path}}'
        helm:
          valueFiles: [values.yaml, values-azure.yaml]
      destination:
        server: https://kubernetes.default.svc
        namespace: scholarpath
      syncPolicy:
        automated: { prune: true, selfHeal: true }
        syncOptions: [CreateNamespace=true, ServerSideApply=true]
