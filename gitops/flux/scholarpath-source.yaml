apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: scholarpath, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/ScholarPath.git
  ref: { branch: main }
