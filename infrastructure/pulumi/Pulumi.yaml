name: scholarpath
description: ScholarPath — Pulumi alternative IaC (mirrors infra/terraform/{aws,gcp,azure})
runtime: { name: nodejs, options: { typescript: true } }
backend:
  url: s3://scholarpath-pulumi-state
config:
  scholarpath:env: prod
  scholarpath:cloud: aws
