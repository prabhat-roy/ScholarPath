testkube-api:
  image: { repository: kubeshop/testkube-api-server, tag: 2.1.0 }
  ingress: { enabled: true, hosts: [{ host: testkube.scholarpath.internal }] }
testkube-dashboard:
  enabled: true
  ingress: { enabled: true, hosts: [{ host: testkube-dash.scholarpath.internal }] }
mongodb: { enabled: true }
