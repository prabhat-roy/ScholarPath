---
apiVersion: v1
kind: ResourceQuota
metadata: { name: scholarpath-quota, namespace: scholarpath }
spec:
  hard:
    requests.cpu: "200"
    requests.memory: 400Gi
    limits.cpu: "400"
    limits.memory: 800Gi
    pods: "1000"
    persistentvolumeclaims: "100"
---
apiVersion: v1
kind: LimitRange
metadata: { name: scholarpath-limits, namespace: scholarpath }
spec:
  limits:
    - type: Container
      default:        { cpu: 500m, memory: 512Mi }
      defaultRequest: { cpu: 50m,  memory: 64Mi }
      max:            { cpu: 4,    memory: 4Gi }
