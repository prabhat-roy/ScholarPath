apiVersion: v1
kind: Service
metadata:
  name: waitlist-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: waitlist-service, scholarpath.io/domain: enrolment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50041, targetPort: 50041 }]
  selector: { app.kubernetes.io/name: waitlist-service }
