apiVersion: apps/v1
kind: Deployment
metadata:
  name: waitlist-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: waitlist-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: enrolment
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: waitlist-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: waitlist-service, scholarpath.io/domain: enrolment }
    spec:
      serviceAccountName: waitlist-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: waitlist-service
          image: harbor.scholarpath.internal/scholarpath/waitlist-service:latest
          ports: [{ containerPort: 50041, name: http }]
          envFrom: [{ configMapRef: { name: waitlist-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50041 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50041 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
