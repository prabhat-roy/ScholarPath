apiVersion: v1
kind: Service
metadata:
  name: admission-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: admission-service, scholarpath.io/domain: enrolment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50040, targetPort: 50040 }]
  selector: { app.kubernetes.io/name: admission-service }
