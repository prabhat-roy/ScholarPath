apiVersion: v1
kind: Service
metadata:
  name: document-verification
  namespace: scholarpath
  labels: { app.kubernetes.io/name: document-verification, scholarpath.io/domain: enrolment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50042, targetPort: 50042 }]
  selector: { app.kubernetes.io/name: document-verification }
