apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: api-key-service, scholarpath.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
