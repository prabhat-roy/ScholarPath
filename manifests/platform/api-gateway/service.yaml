apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: scholarpath
  labels: { app.kubernetes.io/name: api-gateway, scholarpath.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50000, targetPort: 50000 }]
  selector: { app.kubernetes.io/name: api-gateway }
