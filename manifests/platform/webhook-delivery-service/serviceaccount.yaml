apiVersion: v1
kind: ServiceAccount
metadata:
  name: webhook-delivery-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: webhook-delivery-service, scholarpath.io/domain: platform }
