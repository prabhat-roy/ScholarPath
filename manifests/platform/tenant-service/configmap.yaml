apiVersion: v1
kind: ConfigMap
metadata:
  name: tenant-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: tenant-service, scholarpath.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51006"
