apiVersion: v1
kind: ConfigMap
metadata:
  name: event-replay-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: event-replay-service, scholarpath.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51009"
