apiVersion: v1
kind: Service
metadata:
  name: file-upload-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: file-upload-service, scholarpath.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51011, targetPort: 51011 }]
  selector: { app.kubernetes.io/name: file-upload-service }
