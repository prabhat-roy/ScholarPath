apiVersion: v1
kind: Service
metadata:
  name: parent-sso
  namespace: scholarpath
  labels: { app.kubernetes.io/name: parent-sso, scholarpath.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50022, targetPort: 50022 }]
  selector: { app.kubernetes.io/name: parent-sso }
