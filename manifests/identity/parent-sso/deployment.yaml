apiVersion: apps/v1
kind: Deployment
metadata:
  name: parent-sso
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: parent-sso
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: parent-sso } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: parent-sso, scholarpath.io/domain: identity }
    spec:
      serviceAccountName: parent-sso
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: parent-sso
          image: harbor.scholarpath.internal/scholarpath/parent-sso:latest
          ports: [{ containerPort: 50022, name: http }]
          envFrom: [{ configMapRef: { name: parent-sso } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50022 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50022 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
