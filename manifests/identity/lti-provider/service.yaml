apiVersion: v1
kind: Service
metadata:
  name: lti-provider
  namespace: scholarpath
  labels: { app.kubernetes.io/name: lti-provider, scholarpath.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50023, targetPort: 50023 }]
  selector: { app.kubernetes.io/name: lti-provider }
