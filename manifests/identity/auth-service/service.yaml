apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: auth-service, scholarpath.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50100, targetPort: 50100 }]
  selector: { app.kubernetes.io/name: auth-service }
