apiVersion: v1
kind: Service
metadata:
  name: digital-library
  namespace: scholarpath
  labels: { app.kubernetes.io/name: digital-library, scholarpath.io/domain: library }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: digital-library }
