apiVersion: v1
kind: Service
metadata:
  name: e-book-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: e-book-service, scholarpath.io/domain: library }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: e-book-service }
