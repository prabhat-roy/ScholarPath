apiVersion: v1
kind: Service
metadata:
  name: media-streaming
  namespace: scholarpath
  labels: { app.kubernetes.io/name: media-streaming, scholarpath.io/domain: library }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50223, targetPort: 50223 }]
  selector: { app.kubernetes.io/name: media-streaming }
