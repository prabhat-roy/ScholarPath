apiVersion: v1
kind: Service
metadata:
  name: anti-cheat
  namespace: scholarpath
  labels: { app.kubernetes.io/name: anti-cheat, scholarpath.io/domain: assessment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50124, targetPort: 50124 }]
  selector: { app.kubernetes.io/name: anti-cheat }
