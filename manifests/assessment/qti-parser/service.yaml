apiVersion: v1
kind: Service
metadata:
  name: qti-parser
  namespace: scholarpath
  labels: { app.kubernetes.io/name: qti-parser, scholarpath.io/domain: assessment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: qti-parser }
