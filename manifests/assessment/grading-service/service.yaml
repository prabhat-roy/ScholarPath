apiVersion: v1
kind: Service
metadata:
  name: grading-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: grading-service, scholarpath.io/domain: assessment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50123, targetPort: 50123 }]
  selector: { app.kubernetes.io/name: grading-service }
