apiVersion: v1
kind: Service
metadata:
  name: proctoring-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: proctoring-service, scholarpath.io/domain: assessment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: proctoring-service }
