apiVersion: apps/v1
kind: Deployment
metadata:
  name: proctoring-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: proctoring-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: assessment
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: proctoring-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: proctoring-service, scholarpath.io/domain: assessment }
    spec:
      serviceAccountName: proctoring-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: proctoring-service
          image: harbor.scholarpath.internal/scholarpath/proctoring-service:latest
          ports: [{ containerPort: 50121, name: http }]
          envFrom: [{ configMapRef: { name: proctoring-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
