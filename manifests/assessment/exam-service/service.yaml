apiVersion: v1
kind: Service
metadata:
  name: exam-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: exam-service, scholarpath.io/domain: assessment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50600, targetPort: 50600 }]
  selector: { app.kubernetes.io/name: exam-service }
