apiVersion: apps/v1
kind: Deployment
metadata:
  name: polling-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: polling-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: classroom
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: polling-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: polling-service, scholarpath.io/domain: classroom }
    spec:
      serviceAccountName: polling-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: polling-service
          image: harbor.scholarpath.internal/scholarpath/polling-service:latest
          ports: [{ containerPort: 50082, name: http }]
          envFrom: [{ configMapRef: { name: polling-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
