apiVersion: v1
kind: Service
metadata:
  name: polling-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: polling-service, scholarpath.io/domain: classroom }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50082, targetPort: 50082 }]
  selector: { app.kubernetes.io/name: polling-service }
