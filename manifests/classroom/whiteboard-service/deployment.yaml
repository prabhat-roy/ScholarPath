apiVersion: apps/v1
kind: Deployment
metadata:
  name: whiteboard-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: whiteboard-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: classroom
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: whiteboard-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: whiteboard-service, scholarpath.io/domain: classroom }
    spec:
      serviceAccountName: whiteboard-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: whiteboard-service
          image: harbor.scholarpath.internal/scholarpath/whiteboard-service:latest
          ports: [{ containerPort: 50081, name: http }]
          envFrom: [{ configMapRef: { name: whiteboard-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
