apiVersion: v1
kind: Service
metadata:
  name: breakout-rooms
  namespace: scholarpath
  labels: { app.kubernetes.io/name: breakout-rooms, scholarpath.io/domain: classroom }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: breakout-rooms }
