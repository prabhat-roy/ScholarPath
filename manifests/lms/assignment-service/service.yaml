apiVersion: v1
kind: Service
metadata:
  name: assignment-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: assignment-service, scholarpath.io/domain: lms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50100, targetPort: 50100 }]
  selector: { app.kubernetes.io/name: assignment-service }
