apiVersion: apps/v1
kind: Deployment
metadata:
  name: submission-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: submission-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: lms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: submission-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: submission-service, scholarpath.io/domain: lms }
    spec:
      serviceAccountName: submission-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: submission-service
          image: harbor.scholarpath.internal/scholarpath/submission-service:latest
          ports: [{ containerPort: 50101, name: http }]
          envFrom: [{ configMapRef: { name: submission-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
