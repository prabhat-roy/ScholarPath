apiVersion: apps/v1
kind: Deployment
metadata:
  name: rubric-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: rubric-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: lms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: rubric-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: rubric-service, scholarpath.io/domain: lms }
    spec:
      serviceAccountName: rubric-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: rubric-service
          image: harbor.scholarpath.internal/scholarpath/rubric-service:latest
          ports: [{ containerPort: 50102, name: http }]
          envFrom: [{ configMapRef: { name: rubric-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
