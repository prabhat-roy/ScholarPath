apiVersion: v1
kind: Service
metadata:
  name: rubric-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: rubric-service, scholarpath.io/domain: lms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: rubric-service }
