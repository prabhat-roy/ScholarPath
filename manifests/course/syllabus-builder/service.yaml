apiVersion: v1
kind: Service
metadata:
  name: syllabus-builder
  namespace: scholarpath
  labels: { app.kubernetes.io/name: syllabus-builder, scholarpath.io/domain: course }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50062, targetPort: 50062 }]
  selector: { app.kubernetes.io/name: syllabus-builder }
