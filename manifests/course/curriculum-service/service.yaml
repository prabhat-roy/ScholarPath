apiVersion: v1
kind: Service
metadata:
  name: curriculum-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: curriculum-service, scholarpath.io/domain: course }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: curriculum-service }
