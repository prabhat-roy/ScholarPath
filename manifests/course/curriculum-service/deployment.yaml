apiVersion: apps/v1
kind: Deployment
metadata:
  name: curriculum-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: curriculum-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: course
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: curriculum-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: curriculum-service, scholarpath.io/domain: course }
    spec:
      serviceAccountName: curriculum-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: curriculum-service
          image: harbor.scholarpath.internal/scholarpath/curriculum-service:latest
          ports: [{ containerPort: 50060, name: http }]
          envFrom: [{ configMapRef: { name: curriculum-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50060 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50060 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
