apiVersion: v1
kind: Service
metadata:
  name: content-library
  namespace: scholarpath
  labels: { app.kubernetes.io/name: content-library, scholarpath.io/domain: course }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50061, targetPort: 50061 }]
  selector: { app.kubernetes.io/name: content-library }
