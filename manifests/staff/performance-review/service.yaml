apiVersion: v1
kind: Service
metadata:
  name: performance-review
  namespace: scholarpath
  labels: { app.kubernetes.io/name: performance-review, scholarpath.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: performance-review }
