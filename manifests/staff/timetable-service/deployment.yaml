apiVersion: apps/v1
kind: Deployment
metadata:
  name: timetable-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: timetable-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: staff
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: timetable-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: timetable-service, scholarpath.io/domain: staff }
    spec:
      serviceAccountName: timetable-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: timetable-service
          image: harbor.scholarpath.internal/scholarpath/timetable-service:latest
          ports: [{ containerPort: 50240, name: http }]
          envFrom: [{ configMapRef: { name: timetable-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
