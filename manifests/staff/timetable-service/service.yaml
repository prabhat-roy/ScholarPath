apiVersion: v1
kind: Service
metadata:
  name: timetable-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: timetable-service, scholarpath.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: timetable-service }
