apiVersion: apps/v1
kind: Deployment
metadata:
  name: leave-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: leave-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: staff
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: leave-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: leave-service, scholarpath.io/domain: staff }
    spec:
      serviceAccountName: leave-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: leave-service
          image: harbor.scholarpath.internal/scholarpath/leave-service:latest
          ports: [{ containerPort: 50243, name: http }]
          envFrom: [{ configMapRef: { name: leave-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
