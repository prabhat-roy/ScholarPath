apiVersion: v1
kind: Service
metadata:
  name: content-recommendation
  namespace: scholarpath
  labels: { app.kubernetes.io/name: content-recommendation, scholarpath.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: content-recommendation }
