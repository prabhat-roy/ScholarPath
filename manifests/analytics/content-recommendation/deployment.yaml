apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-recommendation
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: content-recommendation
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: content-recommendation } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: content-recommendation, scholarpath.io/domain: analytics }
    spec:
      serviceAccountName: content-recommendation
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: content-recommendation
          image: harbor.scholarpath.internal/scholarpath/content-recommendation:latest
          ports: [{ containerPort: 50262, name: http }]
          envFrom: [{ configMapRef: { name: content-recommendation } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
