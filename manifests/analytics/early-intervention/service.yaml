apiVersion: v1
kind: Service
metadata:
  name: early-intervention
  namespace: scholarpath
  labels: { app.kubernetes.io/name: early-intervention, scholarpath.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: early-intervention }
