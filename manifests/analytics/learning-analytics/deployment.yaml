apiVersion: apps/v1
kind: Deployment
metadata:
  name: learning-analytics
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: learning-analytics
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: learning-analytics } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: learning-analytics, scholarpath.io/domain: analytics }
    spec:
      serviceAccountName: learning-analytics
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: learning-analytics
          image: harbor.scholarpath.internal/scholarpath/learning-analytics:latest
          ports: [{ containerPort: 50260, name: http }]
          envFrom: [{ configMapRef: { name: learning-analytics } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
