apiVersion: v1
kind: Service
metadata:
  name: learning-analytics
  namespace: scholarpath
  labels: { app.kubernetes.io/name: learning-analytics, scholarpath.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: learning-analytics }
