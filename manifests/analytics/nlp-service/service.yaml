apiVersion: v1
kind: Service
metadata:
  name: nlp-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: nlp-service, scholarpath.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50263, targetPort: 50263 }]
  selector: { app.kubernetes.io/name: nlp-service }
