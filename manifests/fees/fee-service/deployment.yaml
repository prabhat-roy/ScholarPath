apiVersion: apps/v1
kind: Deployment
metadata:
  name: fee-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: fee-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: fees
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fee-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fee-service, scholarpath.io/domain: fees }
    spec:
      serviceAccountName: fee-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fee-service
          image: harbor.scholarpath.internal/scholarpath/fee-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: fee-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
