apiVersion: v1
kind: Service
metadata:
  name: fee-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: fee-service, scholarpath.io/domain: fees }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: fee-service }
