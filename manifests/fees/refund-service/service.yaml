apiVersion: v1
kind: Service
metadata:
  name: refund-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: refund-service, scholarpath.io/domain: fees }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50203, targetPort: 50203 }]
  selector: { app.kubernetes.io/name: refund-service }
