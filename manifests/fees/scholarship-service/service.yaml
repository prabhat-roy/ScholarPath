apiVersion: v1
kind: Service
metadata:
  name: scholarship-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: scholarship-service, scholarpath.io/domain: fees }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: scholarship-service }
