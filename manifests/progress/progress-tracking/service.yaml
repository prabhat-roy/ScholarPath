apiVersion: v1
kind: Service
metadata:
  name: progress-tracking
  namespace: scholarpath
  labels: { app.kubernetes.io/name: progress-tracking, scholarpath.io/domain: progress }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: progress-tracking }
