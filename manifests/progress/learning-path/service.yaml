apiVersion: v1
kind: Service
metadata:
  name: learning-path
  namespace: scholarpath
  labels: { app.kubernetes.io/name: learning-path, scholarpath.io/domain: progress }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: learning-path }
