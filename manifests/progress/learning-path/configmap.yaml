apiVersion: v1
kind: ConfigMap
metadata:
  name: learning-path
  namespace: scholarpath
  labels: { app.kubernetes.io/name: learning-path, scholarpath.io/domain: progress }
data:
  LOG_LEVEL: info
  PORT: "50161"
