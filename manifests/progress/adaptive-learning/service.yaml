apiVersion: v1
kind: Service
metadata:
  name: adaptive-learning
  namespace: scholarpath
  labels: { app.kubernetes.io/name: adaptive-learning, scholarpath.io/domain: progress }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: adaptive-learning }
