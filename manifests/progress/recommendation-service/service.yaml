apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: recommendation-service, scholarpath.io/domain: progress }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: recommendation-service }
