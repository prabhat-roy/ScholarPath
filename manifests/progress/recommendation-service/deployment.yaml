apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: recommendation-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: progress
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: recommendation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: recommendation-service, scholarpath.io/domain: progress }
    spec:
      serviceAccountName: recommendation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: recommendation-service
          image: harbor.scholarpath.internal/scholarpath/recommendation-service:latest
          ports: [{ containerPort: 50162, name: http }]
          envFrom: [{ configMapRef: { name: recommendation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50162 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50162 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
