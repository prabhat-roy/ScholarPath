apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: messaging-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: communications
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: messaging-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: messaging-service, scholarpath.io/domain: communications }
    spec:
      serviceAccountName: messaging-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: messaging-service
          image: harbor.scholarpath.internal/scholarpath/messaging-service:latest
          ports: [{ containerPort: 50180, name: http }]
          envFrom: [{ configMapRef: { name: messaging-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
