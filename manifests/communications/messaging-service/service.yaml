apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: messaging-service, scholarpath.io/domain: communications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: messaging-service }
