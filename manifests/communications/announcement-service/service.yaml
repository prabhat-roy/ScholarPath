apiVersion: v1
kind: Service
metadata:
  name: announcement-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: announcement-service, scholarpath.io/domain: communications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50181, targetPort: 50181 }]
  selector: { app.kubernetes.io/name: announcement-service }
