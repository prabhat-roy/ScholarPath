apiVersion: apps/v1
kind: Deployment
metadata:
  name: announcement-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: announcement-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: communications
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: announcement-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: announcement-service, scholarpath.io/domain: communications }
    spec:
      serviceAccountName: announcement-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: announcement-service
          image: harbor.scholarpath.internal/scholarpath/announcement-service:latest
          ports: [{ containerPort: 50181, name: http }]
          envFrom: [{ configMapRef: { name: announcement-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
