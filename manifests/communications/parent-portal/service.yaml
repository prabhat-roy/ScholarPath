apiVersion: v1
kind: Service
metadata:
  name: parent-portal
  namespace: scholarpath
  labels: { app.kubernetes.io/name: parent-portal, scholarpath.io/domain: communications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: parent-portal }
