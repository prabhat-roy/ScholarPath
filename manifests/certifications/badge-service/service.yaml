apiVersion: v1
kind: Service
metadata:
  name: badge-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: badge-service, scholarpath.io/domain: certifications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50140, targetPort: 50140 }]
  selector: { app.kubernetes.io/name: badge-service }
