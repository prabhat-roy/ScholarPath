apiVersion: apps/v1
kind: Deployment
metadata:
  name: badge-service
  namespace: scholarpath
  labels:
    app.kubernetes.io/name: badge-service
    app.kubernetes.io/part-of: scholarpath
    scholarpath.io/domain: certifications
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: badge-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: badge-service, scholarpath.io/domain: certifications }
    spec:
      serviceAccountName: badge-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: badge-service
          image: harbor.scholarpath.internal/scholarpath/badge-service:latest
          ports: [{ containerPort: 50140, name: http }]
          envFrom: [{ configMapRef: { name: badge-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
