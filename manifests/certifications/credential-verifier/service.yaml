apiVersion: v1
kind: Service
metadata:
  name: credential-verifier
  namespace: scholarpath
  labels: { app.kubernetes.io/name: credential-verifier, scholarpath.io/domain: certifications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: credential-verifier }
