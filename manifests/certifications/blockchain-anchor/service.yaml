apiVersion: v1
kind: Service
metadata:
  name: blockchain-anchor
  namespace: scholarpath
  labels: { app.kubernetes.io/name: blockchain-anchor, scholarpath.io/domain: certifications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: blockchain-anchor }
