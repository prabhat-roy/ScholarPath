apiVersion: v1
kind: Service
metadata:
  name: data-retention
  namespace: scholarpath
  labels: { app.kubernetes.io/name: data-retention, scholarpath.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: data-retention }
