apiVersion: v1
kind: Service
metadata:
  name: ferpa-service
  namespace: scholarpath
  labels: { app.kubernetes.io/name: ferpa-service, scholarpath.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: ferpa-service }
