apiVersion: v1
kind: Service
metadata:
  name: coppa-consent
  namespace: scholarpath
  labels: { app.kubernetes.io/name: coppa-consent, scholarpath.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: coppa-consent }
