apiVersion: v1
kind: Service
metadata:
  name: safeguarding-alerts
  namespace: scholarpath
  labels: { app.kubernetes.io/name: safeguarding-alerts, scholarpath.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50283, targetPort: 50283 }]
  selector: { app.kubernetes.io/name: safeguarding-alerts }
