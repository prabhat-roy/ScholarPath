apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: scholarpath-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [scholarpath] }
  timeOffset: "+500ms"
  duration: 3m
