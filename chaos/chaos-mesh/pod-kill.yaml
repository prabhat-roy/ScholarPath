apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata: { name: scholarpath-pod-kill, namespace: chaos-mesh }
spec:
  action: pod-kill
  mode: one
  selector: { namespaces: [scholarpath] }
  duration: 1m
  scheduler: { cron: "@every 1h" }
