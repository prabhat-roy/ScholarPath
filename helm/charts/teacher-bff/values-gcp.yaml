cloud: { provider: gcp, storageClass: pd-balanced, ingressClass: gce }
serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: scholarpath-teacher-bff@{{ .Values.gcp.project }}.iam.gserviceaccount.com
envFrom:
  - secretRef: { name: teacher-bff-gcp-secrets }
