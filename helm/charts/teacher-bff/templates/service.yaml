apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-teacher-bff
  labels: { app.kubernetes.io/name: teacher-bff, scholarpath.io/domain: platform }
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, name: http }]
  selector: { app.kubernetes.io/name: teacher-bff }
