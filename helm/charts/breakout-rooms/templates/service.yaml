apiVersion: v1
kind: Service
metadata:
  name: {{ include "breakout-rooms.fullname" . }}
  labels: {{- include "breakout-rooms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "breakout-rooms.selectorLabels" . | nindent 4 }}
