apiVersion: v1
kind: Service
metadata:
  name: {{ include "performance-review.fullname" . }}
  labels: {{- include "performance-review.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "performance-review.selectorLabels" . | nindent 4 }}
