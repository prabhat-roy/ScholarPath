apiVersion: v1
kind: Service
metadata:
  name: {{ include "adaptive-learning.fullname" . }}
  labels: {{- include "adaptive-learning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "adaptive-learning.selectorLabels" . | nindent 4 }}
