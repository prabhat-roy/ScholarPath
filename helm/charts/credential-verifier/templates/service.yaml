apiVersion: v1
kind: Service
metadata:
  name: {{ include "credential-verifier.fullname" . }}
  labels: {{- include "credential-verifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "credential-verifier.selectorLabels" . | nindent 4 }}
