apiVersion: v1
kind: Service
metadata:
  name: {{ include "progress-tracking.fullname" . }}
  labels: {{- include "progress-tracking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "progress-tracking.selectorLabels" . | nindent 4 }}
