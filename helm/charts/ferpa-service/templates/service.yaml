apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferpa-service.fullname" . }}
  labels: {{- include "ferpa-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ferpa-service.selectorLabels" . | nindent 4 }}
