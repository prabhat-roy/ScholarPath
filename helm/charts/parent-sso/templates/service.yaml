apiVersion: v1
kind: Service
metadata:
  name: {{ include "parent-sso.fullname" . }}
  labels: {{- include "parent-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "parent-sso.selectorLabels" . | nindent 4 }}
