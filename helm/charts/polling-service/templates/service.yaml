apiVersion: v1
kind: Service
metadata:
  name: {{ include "polling-service.fullname" . }}
  labels: {{- include "polling-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "polling-service.selectorLabels" . | nindent 4 }}
