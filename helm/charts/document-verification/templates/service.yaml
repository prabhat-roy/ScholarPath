apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-verification.fullname" . }}
  labels: {{- include "document-verification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "document-verification.selectorLabels" . | nindent 4 }}
