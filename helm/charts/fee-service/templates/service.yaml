apiVersion: v1
kind: Service
metadata:
  name: {{ include "fee-service.fullname" . }}
  labels: {{- include "fee-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fee-service.selectorLabels" . | nindent 4 }}
