apiVersion: v1
kind: Service
metadata:
  name: {{ include "waitlist-service.fullname" . }}
  labels: {{- include "waitlist-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "waitlist-service.selectorLabels" . | nindent 4 }}
