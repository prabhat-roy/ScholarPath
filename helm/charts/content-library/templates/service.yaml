apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-library.fullname" . }}
  labels: {{- include "content-library.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "content-library.selectorLabels" . | nindent 4 }}
