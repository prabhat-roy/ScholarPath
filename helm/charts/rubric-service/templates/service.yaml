apiVersion: v1
kind: Service
metadata:
  name: {{ include "rubric-service.fullname" . }}
  labels: {{- include "rubric-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rubric-service.selectorLabels" . | nindent 4 }}
