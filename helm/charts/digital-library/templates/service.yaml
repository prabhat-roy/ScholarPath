apiVersion: v1
kind: Service
metadata:
  name: {{ include "digital-library.fullname" . }}
  labels: {{- include "digital-library.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "digital-library.selectorLabels" . | nindent 4 }}
