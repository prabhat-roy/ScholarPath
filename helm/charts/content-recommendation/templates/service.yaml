apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-recommendation.fullname" . }}
  labels: {{- include "content-recommendation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "content-recommendation.selectorLabels" . | nindent 4 }}
