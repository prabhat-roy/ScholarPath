apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-bff.fullname" . }}
  labels: {{- include "admin-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "admin-bff.selectorLabels" . | nindent 4 }}
