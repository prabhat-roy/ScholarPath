apiVersion: v1
kind: Service
metadata:
  name: {{ include "syllabus-builder.fullname" . }}
  labels: {{- include "syllabus-builder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "syllabus-builder.selectorLabels" . | nindent 4 }}
