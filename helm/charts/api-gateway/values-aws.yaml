# AWS overlay for api-gateway.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/scholarpath-api-gateway

podAnnotations:
  iam.amazonaws.com/role: scholarpath-api-gateway

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: api-gateway-aws-secrets
