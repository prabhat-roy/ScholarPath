apiVersion: v1
kind: Service
metadata:
  name: {{ include "student-sso.fullname" . }}
  labels: {{- include "student-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "student-sso.selectorLabels" . | nindent 4 }}
