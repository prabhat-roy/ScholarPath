apiVersion: v1
kind: Service
metadata:
  name: {{ include "nlp-service.fullname" . }}
  labels: {{- include "nlp-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "nlp-service.selectorLabels" . | nindent 4 }}
