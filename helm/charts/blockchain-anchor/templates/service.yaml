apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockchain-anchor.fullname" . }}
  labels: {{- include "blockchain-anchor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "blockchain-anchor.selectorLabels" . | nindent 4 }}
