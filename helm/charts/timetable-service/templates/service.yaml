apiVersion: v1
kind: Service
metadata:
  name: {{ include "timetable-service.fullname" . }}
  labels: {{- include "timetable-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "timetable-service.selectorLabels" . | nindent 4 }}
