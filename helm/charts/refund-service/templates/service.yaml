apiVersion: v1
kind: Service
metadata:
  name: {{ include "refund-service.fullname" . }}
  labels: {{- include "refund-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "refund-service.selectorLabels" . | nindent 4 }}
