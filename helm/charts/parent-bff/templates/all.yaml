apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-parent-bff
  labels: { app.kubernetes.io/name: parent-bff, scholarpath.io/domain: platform }
  {{- with .Values.serviceAccount.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-parent-bff
  labels: { app.kubernetes.io/name: parent-bff, scholarpath.io/domain: platform }
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, name: http }]
  selector: { app.kubernetes.io/name: parent-bff }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-parent-bff
  labels: { app.kubernetes.io/name: parent-bff, app.kubernetes.io/part-of: scholarpath, scholarpath.io/domain: platform }
spec:
  replicas: {{ .Values.replicaCount }}
  selector: { matchLabels: { app.kubernetes.io/name: parent-bff } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: parent-bff, scholarpath.io/domain: platform }
    spec:
      serviceAccountName: {{ .Release.Name }}-parent-bff
      securityContext: {{- toYaml .Values.securityContext | nindent 8 }}
      containers:
        - name: parent-bff
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports: [{ name: http, containerPort: {{ .Values.service.targetPort }} }]
          env:
            - { name: PORT, value: "{{ .Values.service.targetPort }}" }
            {{- range $k, $v := .Values.config }}
            - { name: {{ $k }}, value: "{{ $v }}" }
            {{- end }}
          {{- with .Values.envFrom }}
          envFrom: {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe: {{- toYaml .Values.probes.liveness | nindent 12 }}
          readinessProbe: {{- toYaml .Values.probes.readiness | nindent 12 }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          securityContext: {{- toYaml .Values.containerSecurityContext | nindent 12 }}
