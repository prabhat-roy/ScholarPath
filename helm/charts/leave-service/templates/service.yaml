apiVersion: v1
kind: Service
metadata:
  name: {{ include "leave-service.fullname" . }}
  labels: {{- include "leave-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "leave-service.selectorLabels" . | nindent 4 }}
