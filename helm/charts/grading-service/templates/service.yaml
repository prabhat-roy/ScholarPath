apiVersion: v1
kind: Service
metadata:
  name: {{ include "grading-service.fullname" . }}
  labels: {{- include "grading-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "grading-service.selectorLabels" . | nindent 4 }}
