apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-streaming.fullname" . }}
  labels: {{- include "media-streaming.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "media-streaming.selectorLabels" . | nindent 4 }}
