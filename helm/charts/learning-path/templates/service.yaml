apiVersion: v1
kind: Service
metadata:
  name: {{ include "learning-path.fullname" . }}
  labels: {{- include "learning-path.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "learning-path.selectorLabels" . | nindent 4 }}
