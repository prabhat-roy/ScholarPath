cloud: { provider: aws, storageClass: gp3, ingressClass: alb }
serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/scholarpath-student-bff
podAnnotations:
  iam.amazonaws.com/role: scholarpath-student-bff
envFrom:
  - secretRef: { name: student-bff-aws-secrets }
