apiVersion: v1
kind: Service
metadata:
  name: {{ include "safeguarding-alerts.fullname" . }}
  labels: {{- include "safeguarding-alerts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "safeguarding-alerts.selectorLabels" . | nindent 4 }}
