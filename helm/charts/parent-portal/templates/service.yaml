apiVersion: v1
kind: Service
metadata:
  name: {{ include "parent-portal.fullname" . }}
  labels: {{- include "parent-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "parent-portal.selectorLabels" . | nindent 4 }}
