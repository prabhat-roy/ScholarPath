apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhook-router.fullname" . }}
  labels: {{- include "webhook-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "webhook-router.selectorLabels" . | nindent 4 }}
