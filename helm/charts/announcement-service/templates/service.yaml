apiVersion: v1
kind: Service
metadata:
  name: {{ include "announcement-service.fullname" . }}
  labels: {{- include "announcement-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "announcement-service.selectorLabels" . | nindent 4 }}
