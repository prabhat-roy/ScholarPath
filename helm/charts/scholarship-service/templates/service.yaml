apiVersion: v1
kind: Service
metadata:
  name: {{ include "scholarship-service.fullname" . }}
  labels: {{- include "scholarship-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "scholarship-service.selectorLabels" . | nindent 4 }}
