apiVersion: v1
kind: Service
metadata:
  name: {{ include "feature-flag-proxy.fullname" . }}
  labels: {{- include "feature-flag-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "feature-flag-proxy.selectorLabels" . | nindent 4 }}
