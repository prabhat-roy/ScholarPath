opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: scholarpath-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.scholarpath.internal, paths: [{ path: /, pathType: Prefix }] }]
